apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: observability
spec:
  project: default
  source:
    repoURL: https://github.com/warroyo/vks-argocd-examples
    targetRevision: main
    path: ./observability/source
  destination:
    name: replacme
    namespace: observability
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      limit: 5
      backoff:
        duration: 5s
        maxDuration: 3m0s
        factor: 2
    syncOptions:
    - CreateNamespace=true